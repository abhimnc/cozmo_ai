# Module graph

Generated by `scripts/module_graph.py` from the actual imports, so it cannot
drift from the code. Regenerate after moving a module. Commit `f1467eb`.

**20 modules · 3155 lines · 10 with no project dependencies · no cycles.**

---

## Dependency table

| Module | Lines | Imports | Imported by | Role |
|---|---:|---|---|---|
| `photo/vanishing` | 344 | `photo/lines` | `photo/calibrate` | rejected Manhattan-frame search, retained for `calibrate` |
| `plan` | 317 | `bundle`, `damage/rules`, `damage/scope`, `layout`, `measure`, `photo/openings`, `photo/room`, `render` | `cli` | assembly — composes estimates, layout and damage into the output |
| `cli` | 297 | `budget`, `bundle`, `damage/rules`, `damage/scope`, `lidar/frames`, `photo/adjacency`, `photo/calibrate`, `photo/room`, `plan`, `score`, `video/frames` | — | sequencing — parses the command and orders the stages |
| `photo/room` | 274 | `photo/exif`, `photo/floorplane`, `photo/lines`, `photo/openings` | `cli`, `plan` | domain judgement — decides which fitted line is a wall |
| `photo/openings` | 230 | `photo/floorplane`, `photo/lines` | `photo/room`, `plan` | pairs vertical jambs standing on a wall line |
| `bundle` | 169 | `budget` | `cli`, `lidar/frames`, `plan`, `video/frames` | access — loads a capture; the only file that opens files |
| `photo/floorplane` | 164 | — | `photo/openings`, `photo/room` | back-projects to the floor plane and fits straight runs |
| `damage/rules` | 159 | — | `cli`, `damage/scope`, `plan` | five concealed-damage rules |
| `photo/adjacency` | 159 | — | `cli` | matches images across rooms to recover which rooms touch |
| `layout` | 153 | — | `plan` | solves room positions from sizes and adjacency |
| `score` | 115 | — | `cli` | compares a plan against tape ground truth |
| `video/frames` | 109 | `bundle` | `cli` | decodes per-room frames from a walkthrough clip |
| `measure` | 100 | — | `plan` | **the interval invariant** — no value without an interval |
| `render` | 96 | — | `plan` | plan.json to SVG |
| `photo/calibrate` | 92 | `photo/exif`, `photo/lines`, `photo/vanishing` | `cli` | cross-checks EXIF focal length against image geometry |
| `damage/scope` | 89 | `damage/rules` | `cli`, `plan` | scope line items from damage and flags |
| `budget` | 88 | — | `bundle`, `cli` | **the access invariant** — what a tier may read |
| `photo/lines` | 87 | — | `photo/calibrate`, `photo/openings`, `photo/room`, `photo/vanishing` | line segment detection |
| `photo/exif` | 72 | — | `photo/calibrate`, `photo/room` | camera parameters from a JPEG |
| `lidar/frames` | 41 | `bundle` | `cli` | selects per-room keyframes from a LiDAR capture |

---

## Call flow for `cozmo run`

```
cli.run
 │
 ├─▶ bundle.load ──▶ budget            build the gate from the manifest
 │
 ├─▶ [tier branch] ─┬─ bundle.photos_by_room     photo
 │                  ├─ video/frames              video
 │                  └─ lidar/frames              lidar
 │        all three produce dict[room, list[image path]]
 │
 ├─▶ photo/room.analyse   × every image
 │     ├─▶ photo/exif           lens from EXIF
 │     ├─▶ photo/lines          segments
 │     ├─▶ photo/floorplane     project + fit wall lines
 │     └─▶ photo/openings       jamb pairs
 │
 ├─▶ photo/adjacency      which rooms share visible content
 │
 └─▶ plan.build_plan
       ├─▶ measure         wrap values with intervals
       ├─▶ photo/openings  aggregate across views
       ├─▶ layout          solve room positions
       ├─▶ damage/rules ──▶ damage/scope
       └─▶ render          plan.json ─▶ plan.svg
```

---

## Shape of the graph

**Two orchestrators, everything else a leaf.** `cli` sequences the stages and
`plan` composes the output. Adding a geometry module adds one call to one of
them rather than complexity to both.

**The two files carrying the project's guarantees are the two smallest.**
`budget` (88 lines) enforces that no tier reads outside
its declared budget; `measure` (100 lines) enforces that no
value exists without an interval. Both import nothing. An invariant you can read
in one sitting is one you can trust.

**`photo/room` never touches `bundle` or `budget`.** It takes a path string and
returns a verdict, so it can be exercised against any JPEG on disk with no bundle
scaffolding. Access control happens at the boundary, not scattered through the
geometry.

**`render` reads only the finished plan.** It cannot reach the bundle, the
geometry or the estimator, so the drawing cannot show anything absent from
`plan.json`.

**The tier branch touches three modules and each returns the same type.** That is
what keeps the tiers comparable: differences measure what the input is worth,
not which implementation received more attention.

## Modules with no project dependencies

- `budget` — **the access invariant** — what a tier may read
- `damage/rules` — five concealed-damage rules
- `layout` — solves room positions from sizes and adjacency
- `measure` — **the interval invariant** — no value without an interval
- `photo/adjacency` — matches images across rooms to recover which rooms touch
- `photo/exif` — camera parameters from a JPEG
- `photo/floorplane` — back-projects to the floor plane and fits straight runs
- `photo/lines` — line segment detection
- `render` — plan.json to SVG
- `score` — compares a plan against tape ground truth
